[project]
name = "pattern-demos"
version = "0.1.0"
description = "Interactive demos for AgentGateway patterns"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "rich>=13.0.0",
]

[dependency-groups]
dev = []
