[project]
name = "a2a-client"
version = "0.1.0"
description = "Test client for A2A multi-agent delegation demo"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
